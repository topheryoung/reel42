<button class="control-btn mono-mode-button" id="monoToggle">
  mono <span class="keyboard-shortcut">(m)</span>
</button>

<script>
  import { initModeToggle } from '../../utils/modeToggle';

  initModeToggle({
    buttonId: 'monoToggle',
    className: 'mono',
    storageKey: 'mono',
    hotkey: 'm',
  });
</script>
